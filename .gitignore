# Rust build artifacts
/target

# Large external dependencies (keep locally, not in repository)
/firebase-cpp-sdk/
/codeql-repo/

# CodeQL database (can be regenerated)
/codeql-db/

# CodeQL query results (binary format)
codeql_results/*.bqrs

# Build logs
codeql_create.log
analysis.log
query_run.log

# Environment variables (contains sensitive Firebase credentials)
.env
/.temp/

# Development/analysis artifacts (keep locally)
/analysis_output/
/codeql_queries/
/codeql_results/
*.sh
*.py
REFACTORING_PLAN.md
/docs/
